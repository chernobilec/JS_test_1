'use strict'; // современный режим, где не работают неточности старых станджартов js

let number = 5; // переменная изменяема
const leftBorderWidth = 1; // константа, не меняется

number = 10;
console.log(number);

const obj = {
	a: 50
};

obj.a = 10;

console.log(obj);

console.log(name);
var name = 'Ivan';

{
	var result = 50;
}

console.log(result);

alert(5);
[].push('a');

